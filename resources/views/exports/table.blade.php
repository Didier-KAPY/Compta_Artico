<!DOCTYPE html><html lang="fr"><head><meta charset="UTF-8"><style>
body{font-family:DejaVu Sans,sans-serif;font-size:9px;color:#17251f}.header{text-align:center;border-bottom:2px solid #176b4d;padding-bottom:8px}.header img{max-width:55px;max-height:55px}.company{font-size:15px;font-weight:bold;text-transform:uppercase}.slogan{font-style:italic;font-weight:bold;margin:2px}.contact{font-size:8px;color:#66756e}h1{font-size:18px;margin:10px 0 4px;text-align:center}.period{color:#66756e;margin-bottom:16px;text-align:center}table{width:100%;border-collapse:collapse}th,td{border:1px solid #cfd8d3;padding:6px 5px;vertical-align:top}th{background:#176b4d;color:#fff;text-align:left}tr:nth-child(even) td{background:#f3f7f5}.empty{text-align:center;color:#66756e;padding:18px}.footer{margin-top:12px;color:#66756e;text-align:right}
</style></head><body><div class="header">@if($entreprise?->logo)<img src="{{ public_path('storage/'.$entreprise->logo) }}" alt="">@endif<div class="company">{{ $entreprise?->nom_entreprise ?? 'Entreprise' }}</div>@if($entreprise?->slogan)<div class="slogan">{{ $entreprise->slogan }}</div>@endif<div class="contact">{{ $entreprise?->adresse }} @if($entreprise?->telephone) — Tél. {{ $entreprise->telephone }} @endif</div></div><h1>{{ $titre }}</h1><div class="period">Période du {{ \Carbon\Carbon::parse($dateDebut)->format('d/m/Y') }} au {{ \Carbon\Carbon::parse($dateFin)->format('d/m/Y') }}</div><table><thead><tr>@foreach($headers as $header)<th>{{ $header }}</th>@endforeach</tr></thead><tbody>@forelse($rows as $row)<tr>@foreach($row as $cell)<td>{{ $cell }}</td>@endforeach</tr>@empty<tr><td colspan="{{ count($headers) }}" class="empty">Aucune donnée pour cette période.</td></tr>@endforelse</tbody></table><div class="footer">Généré le {{ now()->format('d/m/Y à H:i') }}</div></body></html>
