<!DOCTYPE html><html lang="fr"><head><meta charset="UTF-8"></head><body><table><tr><th colspan="{{ count($headers) }}" style="font-size:18px">{{ $entreprise?->nom_entreprise ?? 'Entreprise' }}</th></tr>@if($entreprise?->slogan)<tr><th colspan="{{ count($headers) }}" style="font-style:italic">{{ $entreprise->slogan }}</th></tr>@endif<tr><th colspan="{{ count($headers) }}">{{ $entreprise?->adresse }} @if($entreprise?->telephone) — Tél. {{ $entreprise->telephone }} @endif</th></tr></table><h2>{{ $titre }}</h2><p>Période du {{ \Carbon\Carbon::parse($dateDebut)->format('d/m/Y') }} au {{ \Carbon\Carbon::parse($dateFin)->format('d/m/Y') }}</p><table border="1"><thead><tr>@foreach($headers as $header)<th style="background:#176b4d;color:#fff">{{ $header }}</th>@endforeach</tr></thead><tbody>@forelse($rows as $row)<tr>@foreach($row as $cell)<td>{{ $cell }}</td>@endforeach</tr>@empty<tr><td colspan="{{ count($headers) }}">Aucune donnée pour cette période.</td></tr>@endforelse</tbody></table></body></html>
