@extends('layouts.app') @section('title','Périodes de paie') @section('module-sidebar') @include('ressources_humaines._sidebar') @endsection @section('content')<div class="container-fluid py-4"><h2>Périodes de paie</h2>@if(session('success'))<div class="alert alert-success">{{session('success')}}</div>@endif @if($errors->any())<div class="alert alert-danger">{{$errors->first()}}</div>@endif <form method="POST" action="{{route('parametres.rh.periodes.store')}}" class="card card-body border-0 shadow-sm row g-2 mb-4">@csrf<div class="row g-2"><div class="col"><input name="libelle" class="form-control" placeholder="Septembre 2026" required></div><div class="col"><input name="date_debut" type="date" class="form-control" required></div><div class="col"><input name="date_fin" type="date" class="form-control" required></div><div class="col"><input name="annee" type="number" value="{{now()->year}}" class="form-control" required></div><div class="col"><input name="mois" type="number" min="1" max="12" value="{{now()->month}}" class="form-control" required></div><div class="col-auto"><button class="btn btn-primary">Ouvrir</button></div></div></form><div class="card border-0 shadow-sm"><table class="table mb-0"><thead><tr><th>Période</th><th>Dates</th><th>Statut</th></tr></thead><tbody>@forelse($periodes as $p)<tr><td>{{$p->libelle}}</td><td>{{$p->date_debut->format('d/m/Y')}} – {{$p->date_fin->format('d/m/Y')}}</td><td>{{$p->statut}}</td></tr>@empty<tr><td colspan="3" class="text-center py-4">Aucune période.</td></tr>@endforelse</tbody></table></div>{{$periodes->links()}}</div>@endsection
